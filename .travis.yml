language: java
sudo: false
script: mvn clean install
notifications: 
    email:
      recipients:
      - rakesh.kanchalwar@bitwiseglobal.com   
    on_success: always
    on_failure: always
    
env:
  global:
    - "FTP_USER=CapOne"
    - "FTP_PASSWORD=!c@pone#"
after_success:
    "find /home/travis/build/rakeshkanchalwar/TestSpringBuild/ -exec curl --ftp-create-dirs -T {}  -u $FTP_USER:$FTP_PASSWORD ftp://ftp.bitwiseglobal.com:4469/$TRAVIS_JOB_NUMBER/ \\"
    
